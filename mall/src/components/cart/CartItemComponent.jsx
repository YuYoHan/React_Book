const CartItemComponent = ({ cino, pName, price, pno, qty, imageFile }) => {
    return (
        <>
            <div>
                {cino} -- {pName}
            </div>
        </>
    );
};

export default CartItemComponent;
